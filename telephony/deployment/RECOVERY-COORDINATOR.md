# VIP Recovery Coordinator Successor

AI-NOTICE:Schema-Version=0.1
AI-NOTICE:License=AGPL-3.0-or-later
AI-NOTICE:Project=VIP

Source/fixture-only delegation tooling. Maintained destination after owner review:
Documents/Projects/VIP/aimee-telephony-integration/deployment. This does not define
parallel routing, identity, authentication, or human acceptance policy. Consume
the current VIP README, component identity and closure ledger at operation time.
No native mutation or runtime gate was performed for this tranche. R12 includes
a read-only current code/launcher capture and protected config byte transfer;
these are disk evidence, not native recovery or loaded-module certification.

## Bound Components

Current packaging uses `--projects-root /absolute/path/to/Projects` to resolve
component acquisitions. The older explicit `--roots` argument remains for
fixtures, but no generated Sonyhal worktree path is a deployment prerequisite.
For the current Crustacea closure, offline verification delegates to the
retained `fa3323b` owner's `verify --inputs ... --closure-root ...` command.
The complete 37-entry package manifest is checked before helper execution.
The existing flag supplies the retained `crustacea/core` directory instead
of a disposable Sonyhal path. No historical noise/taint patcher runs.
The packet's relay is older than the accepted `71794f4` relay: retain the
separate current relay packet and do not apply the older bundled one.
Native recovery dispatch and its prerequisites are unchanged and remain
separately unqualified. Use native PVE snapshots for live deployment recovery,
as prescribed in the assembly README. Historical sections below describe
their dated inputs, not the current offline-verification command.

Rita recovery revision `current-producer-20260914-r2` has 115 identical files
in its private skeleton and inspectable kit, including the current native
configuration. Tessa uses `tessa-standalone-173c3bf-r2/tessa`. The current relay
packet is `aimee-main-voice-71794f4-20260914-r1`; older relay activation sidecars
must not overwrite it. Ava's current packet is
`ava-accepted-four-payload-92e848b-20260914`, covering both engine and memo
guard from the accepted runtime `27b936e`. The JSON inventory binds every
kit file and its Unix mode. Root `DEPLOY.md` is the current operating procedure;
later historical sections remain provenance, not alternate deployment recipes.

Rita current producer: source6b0efd9a6ed9d702fcc5eb8901b76fbae74fa6d4,
binary source dca859df6c0ccd87508d372b836ed8f926471e44 and installed binary
eeb7bc0265b51be0531ab504640876beaa248a4f8864a1f5bdb731cbe804318f.
The exact producer wrapper delegates `--kit`, `--expected-binary`,
`--expected-unit` and `--expected-environment`; apply adds only `--apply`.
Older-reader rollback is rejected before native transport and reservations are
never rewound. The separately captured native
unit11267c4d3f3a581c69f131587ea33c52602a3e6ca615add3cd4da038fb3b88ee,
not its different public template, is recovery authority. Its current empty
drop-ins, aimee-pbx-router user/group, asterisk supplementary group, StateDirectory
and writable /var/lib/avr-voicemail are prerequisites. Creating an absent owned
state directory differs from preserving an existing ledger; never wipe it.
The protected four-member capture at
`Documents/Projects/Rita/staging/current-producer-config-20260914` is verified
by archive/member hashes, sizes, modes and uid/gid without reading or emitting
secret contents. It closes the missing-current-environment gap but is private,
explicit total-loss recovery material only: never ordinary rollback, config
overwrite or ledger restore. Environment
9af2aa15410c831f8ef9c421edf2e9845dbf2c1518c445c163a7ef1b4e20d56e permits exactly the selector
`AVRIL_AIMEE_TTS_URL=http://ava.bajaj.com:6013/text-to-speech-stream`. If this
protected current capture is absent, verification fails; never synthesize it.

CURRENT LIVE Ava: parent runtime/source52768f25309b81bb1d5d67a25d8b936c54b749dd,
helper6f1788ae87185e295f89f01f0f3f5536f0a6a14b and installed guard
f1b2ce1fece75c6c0870c82c8266f8c82a7fc1df8aece96d350af1bc0f07e0be
mode0644 uid/gid1001. Its preimage was2cb7de41b528e72f3bf054001c790f4e775e941c38c54801c51940402a78732e.
The helper changes ONLY
core/pipeline_message_deposit.py, preserving the bound engine/context/HTTP
foundation. The prior fb package is historical, not this successor's guard.
Unknown native guard hashes fail closed even when supplied as fresh preimages.
The exact retained helper
3b6b0f269d17527a298767ed5aa5af2778afecd4fe36919fa8a5dcec200e26e7
directly provides `--check-only`, `--apply --backup UNIQUE`, `--rollback BACKUP`
and nonactuating `--verify-installed`; no recovery overlay or hash-only facade
is used. Installed acceptance requires JSON status installed_verify_pass,
runtime commit52768f2 and exact installed AFTER identity. The complete owner Native gate
retains image/mount/source-bind, installed compilation, health/ARI/authenticated
PBX zero, protected configuration and all-agent logical stability checks.
Foundation hashes are checked around delegation using the existing native
reader. No binding means blocked, never fallback to BEFORE-only --check-only.
The public repository's historical80cdace checkpoint is continuity evidence,
not an instruction to substitute current or future HEAD for installed runtime.

Voice Organ: accepted executor-only helpere9d197df7aa6a854e823c3f6012298f536bcbf81
is an explicit recovery/ overlay on retained a7fcdad2b34c populated kit. It does
not rewrite that sealed kit. Executor ece5669ff13b36a6c1d4ba7a934f018a59b7debbf2eea55d1dd892a98f2e4a26
is Bookworm x86_64 GLIBC2.34 (maximum2.36). Live legacy Python, deployment
snapshots and bundled source remain intentionally distinct; NONE is promoted.
Tessa source173c3bf2d342f00e8556b92c21835cd5332a2ea3 is the current
standalone `/opt/tessa` owner tree: 16 source files, two
deployment files and its README are retained by exact hash and byte count. The
implementation index is f0b1ef0e2b6d532b86da31f3eb5258ea8cd80a42e270d455c0aa5f736c0f89a2.
Manual owner commands run from `/opt/tessa/deployment`: `docker compose build`,
then `docker compose up -d`. This is a supported manual owner recovery procedure,
not proof that Tessa is unusable. Automated transaction/rollback dispatch is not
supported by this coordinator and stops before native transport. The old tessa-avr helpers
are historical recovery only and never dispatched. AVR is standby evidence,
not a mandatory Tessa dependency.

Crustacea captured phase89c461ae642815907c3dd8f4653490979be89ab4 derives from
accepted native-gate phasea7e0a979d5e39ca21beb30f04745fc680ef7c01c and the
bdd99b1 foundation; R12 binds current core2f02f65dc6dce2eb203d2d1a468919d3b471e457.
It restores exact captured core/dependencies and launcher, not an npm rollback or
dependency re-resolution. Four unchanged patchers run in canonical order only
on a private prepared tree, followed by full retained verification. Already
patched captured bytes must remain exact. CSR/direct-silent are not reapplied.
The previous6fd2 package and closure remain historical R11 inputs, not current
restore authority. The current package is independently hash-bound as
`c840aebb3cee7df7b2829864de5d11d0fb5ddc01f4df388715949190373c8ef8`.
Its package.json, launcher and build-info anchors are verified without extracting
dependencies. The live package AI subtree and managed dependencies/plugins are
retained in the current disk capture, not rebuilt or re-resolved.

Current captured ingestion differs from the four-patcher restore contract:
noise/taint change `session-ingestion-6vy4mT7v.mjs`; the two dreaming patchers are
byte-idempotent on `dreaming-phases-C-w6u2U1.mjs` and
`dreaming-narrative-OOe1PAul.mjs`. The second complete fixture pass is idempotent,
but the first is NOT captured-byte-equal. This is an observed owner review gap,
not permission to add patches to production or waive the final byte check.
Do not invoke captured core apply with these current inputs until the existing
owner restore contract is resolved. Offline retention verification can succeed
while `owner_restore_compatible=false`; it is not a restore success claim.

## Whole Package Roots vs Native Stages

../retained-roots.json points to the actual populated Rita and current Ava packages
and the populated owner-input/ packet referencing the current124003503-byte core closure. The closure is verified streaming
against all37886 records, without duplicating its full dependency tree. Current
closure is available, not a missing foundation. It is disk evidence, not loaded
module proof. The operator roots additionally reference the complete Voice Organ
package. Rita's 108-file amended ordinary kit and 114-file historical skeleton
are independently verified and explicitly unequal. Tessa is a 19-file current
owner retention, not a claimed complete four-payload release.
The supplied roots file is operator configuration outside the source commit.

../native-staging-plan.json is a PROPOSED host-local layout, not transfer proof.
Under existing protected /home/aimee/.local/share/vip-recovery-* or /root/vip-recovery-*:
- Rita: current rita-6b0efd9a6ed9/ kit, exact recovery/promote-producer.py,
  actual accepted unit and protected current environment capture.
- Ava: current ava/deploy/ -> deploy/ and ava/src/ -> src/ with exact kit modes
  and owner1001:1001; image91ae and foundation3 remain unchanged. Only the guard
  is installed. The same exact owner6f178 helper serves all four modes.
- Voice Organ: retained voice-organ/ kit; copy deployment/components/voice-organ/deploy/
  into recovery/deploy/ alongside retained deploy/ and artifacts/. Do not replace legacy Python.
- Tessa: map current source/ to /opt/tessa/source and deployment/ to
  /opt/tessa/deployment on Ava. Manual owner Compose build/up is supported;
  only automated coordinator transaction/rollback dispatch is unsupported.
- Crustacea: deployment/components/crustacea/ -> recovery/; copy this frozen
  coordinator's deployment/ and component-bindings-20260914.json -> coordinator/;
  owner-input/ -> owner-packet/. Retain the exact closure separately at its
  recorded native root. Only closure.root may be relocated in INPUTS.json;
  do not change closure filenames, hashes, source contracts or protected pins.
  Create host-local owner-packet/PHASE-SETTINGS.json mode0600 with exactly
  {"ssh_key":"absolute existing protected native fleet key",
   "native_roots":{"ava":"absolute existing host-local current Ava stage"}}.
  This is operator configuration for the existing fleet transport, not a new checker.
  The key and accepted native stage are not created or supplied by this tranche.
  With the separately verified owner overlay, its exact current-state command is:
  sudo -n /usr/bin/python3 -B "$NATIVE_AVA/deploy/deploy-ava-prior-message-reference.py" --verify-installed
  This is operator-invoked read-only evidence, not an installation or restart.

Every delegated helper and executed wrapper is SHA-verified immediately before
execution. Existing native actors: gary@vip.bajaj.com with sudo-n; aimee on Ava,
Avril and Crustacea. No new accounts, sudoers, credentials or SSH workaround.
Operator's Sonyhal key must already exist and be private; Windows transport key
is not presumed to be a Sonyhal file.

## Retained Crustacea Owner Inputs

The complete historical Senior/OC owner receipt1a0e7d55123e0da725e27716ed13e44969a6404c6cf001bf5c5b0987524c16f2
is retained byte-unchanged in owner-input/. Its original87b activation example
is historical; current bindings use the exact6f178 --verify-installed owner mode.
It supplies native contracts, not new prerequisite
tools. INPUTS.json lives in that private directory and uses
schema crustacea-captured-core-phase-v1, base_source bdd99b1827360ce72d00b8d30bc0acc346cb27af.
Required fields consumed directly by frozen load_inputs():

```text
closure: {root:absolute existing closure directory,
 archive:openclaw-installed-2f02f65-current.tar.gz,
 archive_sha256:f8fab62a41fdc351729121c8de2a7eedaa912477950df720bb0d1a774074f63e,
 manifest:archive-manifest.json,
 manifest_sha256:8c5e82050115ea4dfefe5af98215252f0f2368f9d41cf58ee70ab687c0e75476,
 receipt:CAPTURE-RECEIPT.json,
 receipt_sha256:967d8bfa77d4e0a7b49a719248900ed166ee1cd23350623d5922fc529cdc6623}
expected_current_sha256: actual full comparable core+launcher manifest SHA
protected: current exact absolute path -> SHA, metadata retained as owner evidence
native_gate_contract: {kind:vip-owner-native-v1,
 owner_receipt_sha256:1a0e7d55123e0da725e27716ed13e44969a6404c6cf001bf5c5b0987524c16f2}
retention_verifier_sha256:44340e030e40770416bf1dbec9bac5b84efcf5868ed3c22701c70291e38234e5
patchers: ordered [{name,source:packet-relative exact retained script,sha256}]
patch_targets: {session-ingestion:absolute captured dist file,
 dreaming-phases:absolute captured dist file,dreaming-narrative:absolute captured dist file}
workspace: [{source:packet-relative code blob,target:skills/...,
 sha256:approved desired blob,before_sha256:actual existing static blob}]
```

Bound protected paths: /home/aimee/.openclaw/openclaw.json,
/etc/systemd/system/openclaw.service, /etc/aimee-main-voice-relay.env,
/etc/systemd/system/aimee-main-voice-relay.service, plus the target-only stale
skill .openclaw/source-origin.json hash. Four control files have captured
size/mode/UID/GID pins, checked before and after through CoreOwnerGates.
All nine deployable Avril skill files come from verified Git blobs at108ca20b;
their before_sha256 pins match the owner readback. Source-only JS tests are not
runtime targets. Existing target metadata is retained by the captured phase;
the phonetic, model, provider, relay and presence configuration is not rewritten.
The current overlay/plugin generations remain operator owner-acceptance gates;
this tranche does not replace or rewind the persisted registry/index/runtime DB.
For the installed Avril skill, the current owner receipt and matching clean
Crustacea repository bytes are authority. Its byte-different older Codex skill
source-origin.json pointer is provenance only; never restore from that pointer.
Local/native packets must be semantically equal except closure acquisition root
relocation; all archive/manifest/receipt hashes and source contracts remain exact.

There are NO new zero_checker/ready_checker executables. The maintained
coordinator's core-phase entrypoint injects a typed CoreOwnerGates object into
the accepted native-gate interface. The R5 successor fixes the legacy classifier
for the mandatory native skill agents/openai.yaml descriptor, not runtime agent
stores. Its separate 70-test normal/optimized suites retain the state and
symlink exclusions. Zero reuses Native.zero() on VIP and the complete hash-pinned
current Ava source plus owner6f178 --verify-installed gate, NOT the BEFORE-only
activation --check-only. Helper hash is checked immediately before delegation;
nonzero or unexpected success schema blocks the core phase. Existing privileged
Ava actor is aimee via sudo-n; unprivileged PermissionError is not a reason to
chmod protected configuration or change credentials. Service/control/core operations retain root. Plugin
projection explicitly uses native aimee, HOME=/home/aimee and selected workspace
cwd=/home/aimee/.openclaw/workspace through runuser/env/env-C; never root HOME.
Require selected workspace, persisted registry, empty diagnostics, and sorted
semantic before/after equality including trust, roots and dependency status.
The owner's d90e2f projection hash is evidence with its own encoding, not a claim
that this coordinator's serialization produces that hash.

Readiness reuses systemd Job/MainPID/active/running, the hash-pinned installed
verify-all.sh and HTTP200 on both loopback families. Trusted callback errors are
converted to the phase's typed error so post-replacement failure invokes its
durable journal/restore path. Fixture coverage proves that actual CLI path, final
restored bytes and repeat rollback without config/SQLite/state rewind. Receipt
and inventory guidance are not new native deployment authority.

## Plans and Explicit Execution -- Not Run Here

```sh
STAGE=/home/aimee/work/jd-vip-installed-owner-r7-20260914-v8upnn4q/source
ROOTS=/home/aimee/work/jd-vip-installed-owner-r7-20260914-v8upnn4q/evidence/OPERATOR-ROOTS-r7.json
python3 -B "$STAGE/deployment/recovery-coordinator.py" verify --roots "$ROOTS"
python3 -B "$STAGE/deployment/recovery-coordinator.py" install-plan --roots "$ROOTS" --native-roots "$NATIVE_ROOTS" --expected-preimages "$PREIMAGES" --backups "$BACKUPS"
python3 -B "$STAGE/deployment/recovery-coordinator.py" rollback-plan --roots "$ROOTS" --native-roots "$NATIVE_ROOTS" --expected-preimages "$PREIMAGES" --backups "$BACKUPS"
python3 -B "$STAGE/deployment/recovery-coordinator.py" install --component rita --roots "$ROOTS" --native-roots "$NATIVE_ROOTS" --expected-preimages "$PREIMAGES" --backups "$BACKUPS" --checkpoints "$CHECKPOINTS" --ssh-key "$KEY" --receipt "$RECEIPT"
python3 -B "$STAGE/deployment/recovery-coordinator.py" rollback --component voice-organ --roots "$ROOTS" --native-roots "$NATIVE_ROOTS" --expected-preimages "$PREIMAGES" --backups "$BACKUPS" --checkpoints "$CHECKPOINTS" --ssh-key "$KEY" --receipt "$RECEIPT"
```

These are operator templates, never auto-run. Tessa's manual Compose recovery is
supported; only automated coordinator Tessa dispatch is non-executable. Plans include exact
owner argv where supported and remain INCOMPLETE for missing
operator stage/preimages/backups or external foundation contracts. Readiness order
is existing native Ava/Kokoro plus standalone Tessa foundation, compatible Rita
selector producer, then Ava guard promotion; Crustacea remains last. Voice Organ
is independent and Ext7 remains unchanged. Rollback reverses component order,
but Rita older-reader rollback and Tessa rollback remain explicitly unsupported;
state is never rewound.
FreePBX remains explicit UI/manual checkpoint: VMAuth/UUID/AI_AGENT/Stasis, native
routes, mini Ava/Avril selector and prompt assets. Never overwrite generated files.

PREIMAGES for ordinary components is exact target path->SHA. Core uses
{core_manifest_sha256:actual body SHA,skills/...:actual static SHA}. Its rollback
may encounter owned missing/damaged core; protected snapshot/journal authentication
by the owner helper precedes restoration, not candidate verification. After any
successful delegation the coordinator independently observes snapshot/desired
core AND static hashes; rc0 alone cannot report restoration success.

BACKUPS: Rita's producer owns fixed protected root
`/var/backups/aimee-pbx-router-producer-20260914`; the coordinator does not
select a child and exposes no rollback. Ava uses an exclusive runtime-* child
(do not precreate). Voice
Organ and core rollback require {path:actual protected snapshot,manifest_sha256:SHA}.
Legacy unjournalled executor backups remain unsupported without separately observed
provenance; never fabricate manifests. Current-live CAS, durable prepared/
restore_prepared/restored fingerprints and exact captured bytes are owner-helper
responsibilities. Observation timeout/pending job is indeterminate, never retried
or treated as permission for concurrent recovery mutation. Primary and rollback
errors remain separate; private exclusive in-progress/final receipts survive failure.

No env/config/allowlists/ledger/SQLite/session/history rewind. Ava DB capture is
EVIDENCE ONLY; empty generated backup WAL/derived SHM can be excluded without
alteration, nonempty WAL rejects verification until retained. Accepted live state
and newly accrued records survive ordinary rollback. Fresh-host foundation setup,
Jaime managed provider recovery, Bridgette enabled MCP recovery, current owner
native phase acceptance and human acceptance are not certified by this source tranche.

## Explicit Scope and Remaining Foundations

The actual frozen source fixtures cover native aimee plugin argv/HOME/cwd,
wrong workspace rejection, reordered plugin rows, dependency/trust drift, all
component owner argv, captured restore, partial/failure recovery, and nonempty
backup WAL rejection (empty WAL/derived SHM exclusion without alteration).
The earlier Ava fixture TypeError was a test wrapper's wrong check() signature;
fixing the fixture to the actual defaulted API did not change Ava runtime source.

Whole assembly remains INCOMPLETE: no native recovery execution was invoked.
Parent independently accepted the exact owner6f178 helper's read-only current-state
check in Projects/VIP/receipts/ava-guard-live-20260914.md. The earlier
ava-installed-verifier-parent-native-20260914.md records4d7, not6f178.
That acceptance is only the current Ava gate, not recovery, cold-host or human
acceptance; Junior did not execute native transport for this source tranche.
Fresh-host account/group/image/service/provider/MCP foundations remain explicit
operator checkpoints, not permission to overwrite FreePBX or restore old state.
The complete Voice Organ package remains acquired. Tessa's exact current files
and supported manual owner Compose procedure are retained. Its automated
transaction/rollback dispatch is unsupported; fresh-host image foundations and
native recovery proof remain separate. This does not certify native
recovery execution or cold-host setup. Existing
Jaime managed gptoss-aimee-local provider and Bridgette enabled MCP recovery are
dependencies to verify/recover through their owners, not assumed alternatives.
Any later Ava guard requires a binding refresh; unknown native guard bytes
cannot be promoted with these current pins. Consume current closure ledger;
this tooling does not declare human deposit/recording/recall/isolation acceptance.

## Retained External Foundations

External manifests retain their pinned original byte order. Row hashes, exact
coverage, duplicate rejection and path containment are checked without
reserializing them. Canonical component manifests still require sorted rows.

The coordinator separately hash-binds the current relay 19-row source/proof
handoff (source7260147, runtime4f85c1), the current AIgis 53-file packet and its
immutable receipt erratum, the Matrix 11-file generation packet, LocalAI 4.8.2's
22-file recovery candidate and acquisition sidecar, the two-file current
managed-config amendment, and AVR's 32-file native inventory/private archive.
Exact logical acquisition paths and SHA-256 pins are in
component-bindings-20260914.json. These are references, not duplicated package,
model, configuration or secret bytes in public source. The LocalAI source pin is
the reconstructed recipe commit; application 4.8.2 remains distinct. Its model
sidecar verifies acquisition metadata, not downloaded or live model weights.

Keep --roots and --native-roots component-only, unchanged. Supply a separate
operator-owned JSON file using --external-roots. Values are absolute retained
directory paths, not commands or credential values. Exactly these keys are
accepted; each sidecar is a file below its separately selected directory:

```json
{
  "aigis": "/retained/aigis-package-2026.9.4-942-g108ca20b059-20260914",
  "aigis-erratum": "/retained/VIP/receipts",
  "matrix": "/retained/matrix-generation-g-ecd0f1a7214b03c3-20260914-r1",
  "localai": "/retained/localai-4.8.2-recovery-6ef8d6d-20260914",
  "localai-acquisition": "/retained/localai-4.8.2-recovery-6ef8d6d-20260914-model-acquisition-r1",
  "managed-config": "/retained/current-managed-config-20260914-a7a91c99",
  "avr-foundation": "/retained/avr-foundation-private-20260914-ifc4hm4k",
  "relay-current": "/retained/jd-relay-transcript-continuity-20260914/handoff"
}
```

```sh
python3 -B "$STAGE/deployment/recovery-coordinator.py" verify --roots "$ROOTS" --external-roots "$EXTERNAL_ROOTS"
python3 -B "$STAGE/deployment/recovery-coordinator.py" install-plan --roots "$ROOTS" --native-roots "$NATIVE_ROOTS" --external-roots "$EXTERNAL_ROOTS" --expected-preimages "$PREIMAGES" --backups "$BACKUPS"
python3 -B "$STAGE/deployment/recovery-coordinator.py" rollback-plan --roots "$ROOTS" --native-roots "$NATIVE_ROOTS" --external-roots "$EXTERNAL_ROOTS" --expected-preimages "$PREIMAGES" --backups "$BACKUPS"
```

Full offline verify and plans report each foundation as verified_retention_only
or an explicit missing/invalid prerequisite; missing or mismatched input exits
nonzero. Targeted component verify and native install/rollback dispatch do not
gain a new external-input gate. Matrix's archive is streamed only for its hash;
the regular-only component archive parser never sees its four symlinks. Its
inventory hashes bind the parent's retained member proof without extracting or
following any archived link. Sidecar bytes must match: the AIgis erratum does
not rewrite PACKAGE-TESTS or its aggregate manifest. Private config is hashed,
not interpreted or printed.

Verified retention does not execute a fresh-host restoration or close the whole
assembly. Reconstruct native FreePBX through its maintained UI/setup checkpoint;
obtain image/hardware/model foundations and legitimately recovered access through
their owners. Restore the external Matrix generation/index, AIgis services and
credentials, LocalAI route and managed configuration through their existing
owner procedures. Recover AVR standby through its owner only when explicitly
selected, not as a prerequisite for current standalone Tessa. Establish Ava's
Kokoro foundation and supported standalone Tessa before Rita's selector producer;
current relay continuity/full context must precede conversational qualification.
The accepted relay file-only helper is retained under
Projects/openclaw/staging/jd-relay-continuity-activation-20260914:
activate-relay-file.py338982db291d959341e41923e27955e8ac95cc727b52682ee49ec2ac05119a30
and inspect-ava-health.py59fe7809116abca79ea827beca8beb22d9774e492d2ca796cd724d9934870664.
It supports --check --candidate FILE, --apply --candidate FILE and
--rollback --backup OWNED_SNAPSHOT (with optional --backup-root), not a fabricated
--verify-installed flag. Its exact preimage7fb25/postimage4f85 condition and
approved unit/env pins apply; it is not blank-host provisioning and is not
automatically re-applied to the already-installed postimage. Unit/environment/user
foundations remain owner prerequisites; ordinary core restoration preserves the exact current relay
runtime, unit and environment through CAS. Bridgette/Isla owner bindings,
fresh operation preimages/quiescence, genuine protected rollback snapshots and
native/cold-host/human acceptance remain explicit prerequisites, not supplied by
this source patch. No new routing, directory, PIN proof or action policy exists.

Ordinary component rollback remains unchanged: current config, plugin/session
registries, DB rows, ledgers, recordings and newly accrued state are preserved.
The managed a7a91c99 packet remains historical and does NOT match current CAS.
Current config `df5b26d5473880104b27ff12120edb9c776e007d8e3a3fc52d4465d537caaefc`
is bound by `owner-input/CURRENT-CONFIG-AMENDMENT.json` to the separate private
`current-managed-config.tar.gz`, with native mode0600 uid/gid1000 and exact
byte/hash proof. It is for explicit total-loss recovery, never routine rollback.
Native file ownership/modes come from capture metadata/archive, not Windows loose
copies. Retain component/external packets separately; do not copy large model or
package distributions into this coordinator source or rebuild them here.

## Current Acceptance Boundary

Parent current Core evidence is
`Documents/Projects/VIP/receipts/ext6-full-catalog-parent-review-20260914.md`:
reviewed clean build/package,309 focused tests and current native promotion.
R12 does not repeat the build or independently promote it. The reported schedule
latency223seconds is amber accepted latency, not a blocker for this release;
Calendar verification and schedule-reading are explicitly deferred. Answer-contract
work is later. Latest outgoing smoke failed with STTfinal_count0 before model;
that is not a model/tool or current package acceptance pass. Confirmed
message-taking through native deposit to IMAP remains mandatory and unproven.
Extn7, native PIN, provider/model choices, Tessa manual owner recovery, current
relay continuity and all native component dispatch remain unchanged. R11 is
immutable. New canonical source sealing requires approval of one successor
commit; no uncommitted source may be attributed to the R11 commit.

The dated owner receipt is immutable historical evidence. Its configuration
metadata is preserved as `owner_input.historical_config_metadata`. Effective
current configuration metadata in the owner packet is explicitly attributed
through `protected_config_metadata_origin` to `CURRENT-CONFIG-AMENDMENT.json`,
whose identity and native metadata are independently checked. Do not present
the updated packet metadata as an observation made by the dated receipt.
Native owner-gate code and dispatch remain identical to the frozen baseline.

## Current STT Foundation

The separately retained `ava-stt-current` foundation binds Ava source
`6d87dfcec6d4d55422d325337fe2629f558f8131`, its canonical source/manifest,
private skeleton and inspectable kit. Its SHA256SUMS covers 17 retained files,
including the six protected native configuration inputs. The existing external
foundation verifier checks each file; it does not deploy STT or overwrite live
configuration. Supply the matching external root via `--external-roots`.

Use the kit's `ava/deploy-stt.py` as the separate owner-scoped entrypoint.
Its native check and authenticated speech verification passed on Ava. Cutover
expects the retained dependency baseline and requires zero native PBX calls;
already-updated hosts use verify, not another deployment. A full fresh CUDA
dependency rebuild remains independently unqualified. This binding does not
change the Ava call-engine image, guard identity, native dispatch or order.

The STT-only speech result does not pass the human telephony gate. Fresh Extn 6
incoming/outgoing conversation, explicit hangup and confirmed native IMAP
Voicemail deposit remain required, as does post-change Extn 7 preservation.
Schedule answers and the Incident Controller remain outside acceptance.
