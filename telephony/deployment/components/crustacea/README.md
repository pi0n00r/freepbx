# Current-Pin Core and Workspace Recovery Delegation

The bdd99b1 input prototype below is frozen provenance. The current successor
entrypoint is captured-core-phase.py, described in the final section. Do not use
the historical npm package rollback path as current captured-closure recovery.

Source/fixture-only helper set. No native install, npm operation, gateway/service
restart, model call, config change or database restoration was performed.
This does not close whole-assembly or native/human acceptance.

The exact existing operator phase is acquired from Crustacea source
108ca20b05932f058d7cddb8bbfef29c1a598f6c, file
deploy/bajaj/openclaw-core-install.commands, SHA256
5ea4363ee5402b636a5c59eba8e4497953deb2c943fa1a0e1e3ba1f7a71d880d.
The root npm child uses umask022; caller/private checkpoint mask is unchanged.
The phase installs caller CORE_TGZ via npm --prefix /usr, retaining dependencies
through the existing workflow. The copied phase is not modified. Source workflow
HEAD is not evidence of currently loaded gateway bytes.

## Exact Missing Foundation Inputs

The existing owner artifact is available at Documents/Projects/openclaw/
crustacea-2026.9.4-939-gde1fe2ef943-r2/kit/packages/openclaw-2026.9.4.tgz,
SHA256 1a1dc62f6ef705bc2e516588cd6768638637e261c289562fc64e58e3018b912f.
The fresh raw-read owner receipt distinguishes source de1fe2ef94318d976660a7bd0a43d267131e8db9
from compiled runtime 6fd2c52616977226522e759ecbcfcca093e76295.
Parent observed three package/disk anchors equal and the existing active launch;
this is not whole loaded-module or overlay proof. The core package is NOT missing.

The complete accepted before/after/retention packet has not been provided to this
stage. Default verify blocks before native transport or stop. The owner must
bind the available artifact to current runtime/overlay evidence and retain:

- Independently built before/after npm archives bound to exact source commits,
  full installed trees including dependencies and internal links, and the actual
  /usr/bin/openclaw link target. A source archive alone is not an npm rollback
  package. External links need an explicit owner recovery contract.
- The actual accepted read-only verify-all.sh plus retention/loaded-runtime
  evidence, including the four memory overlays, classifier/native tools, Matrix
  and other active external/custom plugins. Installed script presence or a hash
  supplied by an operator is not proof of loaded equivalence or desired policy.
- The existing accepted coordinated VIP/Ava zero-call checker and its exact
  typed invocation/source hash through sanctioned host actors. No newly named
  standalone checker or parallel zero-call system is required or implemented.
  Caller-supplied existing checker contract is reused byte-for-byte.
- Current config/unit/drop-in/plugin/private route hashes and native schema
  prerequisites. The mandatory four protected paths are only a minimum; the
  owner must supply every current external plugin/control dependency. Preserve
  actual Jaime model route, Bridgette tools/auth, full agent workspace/corpus,
  bootstraps, defaults and existing managed secrets. Do not infer these from
  a repository HEAD or fabricate native evidence.
- Existing static skills' before/after source blobs, hashes and host-local target
  paths. Only existing skills files are supported. Fresh-host accounts, runtime,
  native foundation, secrets and new skill installation remain separate owner
  recovery prerequisites.

Packet layout: INPUTS.json, before/after archives, static skill blobs, and exact
owner verify-all.sh, existing coordinated checker, LOADED-IDENTITY.json. INPUTS.json schema
crustacea-core-workspace-recovery-v1 records workflow_source, before/after
{source_commit,archive,sha256,files}, workspace
{source,target,sha256,before_sha256}, protected absolute-path SHA256 values,
bin_link (exact spelling), bin_resolved_target, retention_verifier_sha256,
zero_checker {path,sha256,runner,args} and
loaded_identity_receipt_sha256. LOADED-IDENTITY.json binds before_source_commit,
before_package_sha256, after_source_commit and after_package_sha256 to the owner
evidence. Parsing/hash binding cannot establish authenticity/loaded equivalence:
the packet and read-only helper contracts require independent owner review.
Tests use explicitly dummy packet evidence, never a native certification.
The checker runner is a fixed Python/Bash/sh interpreter plus its protected
existing script and typed inert argv; it is not an unrestricted remote command
or a model-facing route. The actual owner checker may have no args.

Approved npm may spell the native link ../lib/node_modules/openclaw/openclaw.mjs.
That relative spelling is preserved exactly and separately checked to resolve
to /usr/lib/node_modules/openclaw/openclaw.mjs. No link rewrite is performed.

## Operator Commands

Default is offline; operator-invoked execution is explicit and requires the
accepted packet plus existing root privilege. No arbitrary shell/host/model API.
The three helper/phase files must be transferred together and verified against
the final source manifest immediately before invocation.

~~~sh
python3 -B /HOST-STAGE/core-workspace-recovery.py --inputs /OWNER-PACKET/INPUTS.json
python3 -B /HOST-STAGE/core-workspace-recovery.py plan --inputs /OWNER-PACKET/INPUTS.json
sudo -n python3 -B /HOST-STAGE/core-workspace-recovery.py observe --inputs /OWNER-PACKET/INPUTS.json
sudo -n python3 -B /HOST-STAGE/core-workspace-recovery.py apply --inputs /OWNER-PACKET/INPUTS.json
sudo -n python3 -B /HOST-STAGE/core-workspace-recovery.py rollback --inputs /OWNER-PACKET/INPUTS.json --snapshot /root/crustacea-core-recovery-ACTUAL --snapshot-sha256 ACTUAL_CAPTURE_SHA
~~~

Use Crustacea's existing aimee actor/privilege, not the VIP gary actor.
The fixed accepted phase receives only the pinned package filename; no model
argument, env rewrite, tool narrowing or provider migration is introduced.
Package scripts/dependency resolution must be qualified by the existing owner
workflow before release. This tranche performs no dependency install/build.
Both the executed install-core-phase.sh wrapper and its sourced committed phase
are hash-checked immediately before delegation. Native stop/start/npm mutations
have no outer timeout or forced client termination. Read-only state observations
remain bounded; pending/unknown jobs block any subsequent mutation or retry.

## Preservation and Failure Semantics

Before stop: owner verifier and native zero checks, unit job observation,
complete core CAS, protected config hashes, and static workspace generation.
After stop and before each static-file replacement the helper rechecks controls,
job and file generation. Source/helpers are hash-checked before delegation.
Only openclaw.service stops/starts; relay, PBX, Ava, Extn7 and model services are
not modified. State databases/WAL/canonical Documents/Notes/agents are forbidden
payloads and never restored. No ledger/job/session rewind.

New /root/crustacea-core-recovery-* captures are private700 with600 immutable
snapshot, independently retained before package and static before blobs.
Workspace prepared/installed physical identities are durably journaled before/
after rename; final package and static restored bytes/metadata are proved,
including idempotent rollback. Foreign/partial generations refuse restoration.

The existing npm install is NOT an atomic package transaction. A failed/partial
or indeterminate owner phase produces a protected failure receipt and NO second
npm, stop/start retry or automatic rollback. An explicit rollback is allowed
only for a complete known before/after core generation and owner-journalled
workspace. Pending systemd jobs or unknown observations block mutation. Further
crash/partial-core reconstruction requires owner state review/qualified package
workflow; this helper does not pretend to solve that missing contract.
The OC inventory specifically requires exact live core+launcher capture and
four retained dist patchers in the native recovery phase. This packet-input
prototype is NOT accepted for that native phase yet: independently retained
package restoration is not proof of exact captured-tree restoration when
dependency/overlay reproduction is incomplete. The actual installed 35,681-file/
20-link inventory is disk evidence, not an atomic directory or loaded-module
snapshot. Owner acceptance must reconcile dependency/lifecycle/extra-dist
differences and bind the exact capture/patcher recovery contract before execution.

The read-only observe mode reports the actual installed tree digest and static
skill hashes, not a desired/expected image. The coordinator compares a fresh
observation with the verified action/snapshot after helper completion; exit0
alone never proves restored bytes. Observe does not call a model or start/stop
services. The existing owner packet is still required for path/config authority.

Existing aimee-main-voice/install-aimee-main-voice-relay.sh is NOT delegated as a
generic core installer: it owns env/model/unit rewrites and its .bak/ERR rollback.
Do not run it merely to recover core/workspace. Existing core install phase and
its exact scoped-mask tests are retained as provenance/reuse, not loaded proof.

The r2 artifact's relay c6aa3fb/4f14665d and presence ee5448fb/196c8e09 are
historical. Current component readback instead pins relay
ee90478214af77f5849214b3c426843a46e75074 / 7fb25dae896a9eb0d37c99126c6d9f7cdf572d06f1c3322301768fd004f952ed
and presence 108ca20b05932f058d7cddb8bbfef29c1a598f6c /
307005d3a3f5b41dccc1419c3300a0fca7e372f77be611c4b415982cce9e81df.
Blind recovery replay of historical overlays is not permitted by this helper.

## Captured Closure Successor

captured-core-phase.py reuses the accepted base helper's native state/zero
delegation, private writes and static-file journaling. It consumes the owner
installed-closure archive schema. It does not invoke npm, the old before-package
rollback, lifecycle install scripts, dependency re-resolution, or the relay
installer. The base helper bytes remain exactly 569b842f6d0f853a03153f2136d2529cf4b3adf1b1ccb0c937ef52ebe5726353.

Exact parent-captured disk foundation on Sonyhal:
/home/aimee/work/jd-owner-packet-transfer-20260914-QWIN169C/crustacea-core-6fd2-current-20260914.v3QW6Z/

- openclaw-installed-6fd2c526-current.tar.gz: 147525879 bytes,
  717a7eb89dbc5bf68a21b4e11691720059da13787c485fc4e3531234544d43cb.
- archive-manifest.json:
  e70f2df14b68af600e157e629a40efb543045794ea29dd94e24e650462696651.
- CAPTURE-RECEIPT.json:
  65dfb459b1b6dce75635fb24bbe71f692a3249daa03be53fe78f47de44d6e1d3.
- 37886 records: 35681 files, 21 links including the native launcher, 2184
  directories. Contents, modes, owners, links and complete coverage are checked.
  This is retained disk/dependency closure, not proof of loaded-module origins.

The parent also reproduced all four unchanged retained patchers in canonical
order on three verified package chunks, byte-equal to this closure:
session-ingestion 16e01d03e9bf61e4c9f6d5a44a7d6dfb557bdc7e3aea6c3ed52e3cabf50991bb;
dreaming-phases 316f6adb14caff447d3e4915f2ab06840345467f49917b68a4b8617919001ace;
dreaming-narrative 36b58c3d94296fb5493b8f17e5977138654684d42f1f0a73fd8d51a0c01e069e.
The source pins the exact four patcher hashes/order and full verifier at 108ca20b.
Only these four run on prepared staging, then the full verifier; the already
patched closure must remain byte/metadata identical. No CSR/direct-silent patch
is applied. Their accepted compiled contracts are checked by the full verifier.

INPUTS.json schema crustacea-captured-core-phase-v1 has base_source=bdd99b1's
full commit; closure {root,archive,archive_sha256,manifest,manifest_sha256,receipt,
receipt_sha256}; expected_current_sha256 (complete comparable code manifest);
protected (current config/unit/index/overlay control paths and hashes); workspace
{source,target,sha256,before_sha256}; patchers {name,source,sha256} in the exact
four-item order; patch_targets (the exact three absolute dist paths from the
closure); retention_verifier_sha256; zero_checker and ready_checker
{path,sha256,runner,args}. These are copies of existing accepted owner checkers,
not invented services, parallel native checks or a model-facing command route.
The zero checker must cover the established coordinated native VIP/Ava gate;
the existing readiness contract must cover Gateway health on both families,
plugin diagnostics/index preservation, current model/defaults/workspace roles.
Missing checker/control bindings fail with a concrete missing prerequisite.
No loaded proof or execution authority is fabricated by these input fields.

~~~sh
python3 -B /HOST-STAGE/captured-core-phase.py verify --inputs /OWNER-PACKET/INPUTS.json
python3 -B /HOST-STAGE/captured-core-phase.py plan --inputs /OWNER-PACKET/INPUTS.json
sudo -n python3 -B /HOST-STAGE/captured-core-phase.py observe --inputs /OWNER-PACKET/INPUTS.json
sudo -n python3 -B /HOST-STAGE/captured-core-phase.py apply --inputs /OWNER-PACKET/INPUTS.json
sudo -n python3 -B /HOST-STAGE/captured-core-phase.py rollback --inputs /OWNER-PACKET/INPUTS.json --snapshot /root/crustacea-core-recovery-ACTUAL --snapshot-sha256 ACTUAL_CAPTURE_SHA
~~~

Transfer the two Python modules and manifest-verified owner packet together.
Do not transfer a dependency tree or reinstall plugins separately. Candidate
closure root is an explicit absolute retained input; no credentials are swept.
The exact installed launcher spelling ../lib/node_modules/openclaw/openclaw.mjs
is preserved; an identical launcher keeps its original physical generation.

Apply authenticates current full metadata/content CAS, controls, zero calls,
state/jobs and verifier; captures the actual live core/launcher and existing
static skills privately; rehashes physical generations across capture; and
prepares the full desired closure off the live paths. Required free headroom
covers the durable capture and a second prepared tree for failure restoration.
All files/modes/owners/link targets are checked before stopping the owner unit.
The component lock is shared with the accepted base to avoid duplicate writers.

Prepared and retire-prepared physical identities are durable before rename.
After stop and again at replacement boundaries, controls, native idle/jobs and
physical code/static generations are freshly checked. Core/launcher switching
is not atomic as a pair: a missing root is recoverable only with the exact
retire-prepared journal and intact captured generation, never from guessing.
Failure restore has separate primary/rollback errors; pending/unknown jobs or
foreign generations defer without mutation/retry. Systemd stop/start have no
outer timeout. Read-only observation budgets do not imply a mutation completed.

Rollback first checks controls, zero/state and pinned private snapshot/journals,
not the damaged candidate verifier. It restores only captured code and existing
static skill files, preserving newly accrued SQLite/WAL/ledger/config/index and
external plugin generations. Final restored hashes/modes/owners/links plus the
full retained verifier and native owner readiness contract are authoritative;
exit0 alone is insufficient. Restore-prepared/restored records permit repeat
rollback after post-rename fsync failure. The snapshot records helper SHA and
archive pins, not an ephemeral candidate-root identity. Legacy unjournalled
backups remain unsupported, without fabricated provenance.

All current tests are private dummy/native-state fixtures. Exact parent archive
read verification is separate from execution fixtures. No production install,
service command, model call, ledger/config rewind or whole recovery certification.

## Trusted Native Owner Interface Successor

The ab5780b source/fixture packet remains immutable. Current owner receipt
1a0e7d55123e0da725e27716ed13e44969a6404c6cf001bf5c5b0987524c16f2
establishes native methods, NOT file-based zero/readiness executables.
For this interface INPUTS.json uses native_gate_contract:
{kind:vip-owner-native-v1, owner_receipt_sha256:the exact receipt hash}.
Existing closure/protected/patcher/static-workspace fields are unchanged.
No zero_checker/ready_checker objects are required. The maintained coordinator
calls main(argv, native_gates=trusted_owner_object); JSON/model inputs cannot
provide callables. Plain CLI mutations fail closed without the trusted object.
Existing historical file-checker fixtures/entrypoints remain supported.

OwnerNative reuses existing native stop/start/state/full verifier. Its injected
zero/ready/preservation methods must use coordinator Native.zero(), retained Ava
check-only gate, systemd/dual-loopback health, and semantic plugin/control readbacks.
Before apply the semantic preservation projection is recorded in the protected
snapshot. Rollback authenticates that captured projection and journal before
candidate verification; after restore its exact projection is compared again.
No plugin index/config/session database is restored. New tests exercise actual
OwnerNative -> existing native methods -> apply/repeat rollback, not a checker stub
claimed as production compatibility. No production execution occurred.
